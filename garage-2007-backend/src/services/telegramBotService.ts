// Telegram Bot API wrapper
// Implementation in Phase 4

export const telegramBotService = {
  // callBotApi(method, params): generic Bot API call
  // createInvoiceLink(params): Stars invoice
  // answerPreCheckoutQuery(queryId, ok, errorMessage?): must respond within 10 seconds
  // refundStarPayment(userId, telegramPaymentChargeId): refund Stars to user
}
