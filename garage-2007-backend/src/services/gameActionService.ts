// Game action service — process all game actions server-side
// Implementation in Phase 3
// 11 action handlers: purchase_upgrade, hire_worker, purchase_milestone,
// purchase_decoration, toggle_decoration, activate_boost, replace_boost,
// claim_achievement, claim_daily_reward, watch_rewarded_video, trigger_event

export const gameActionService = {
  // processSync(userId, clicks): validate click rate, compute income, update state
  // processAction(userId, type, payload, idempotencyKey): dispatch by type, shared formulas, BalanceLog
}
