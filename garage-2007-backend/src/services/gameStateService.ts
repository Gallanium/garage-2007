// Game state service — authoritative game state management
// Implementation in Phase 3

export const gameStateService = {
  // loadState(userId): DB → compute offline earnings → tick boosts/events → build GameState
  // createInitialState(userId): Create default GameSave for new player
}
